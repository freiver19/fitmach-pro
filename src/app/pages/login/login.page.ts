import { Component } from '@angular/core'; import { AuthService } from '../../services/auth.service'; import { Router } from '@angular/router'; @Component({selector:'app-login', templateUrl:'./login.page.html'}) export class LoginPage{ email=''; password=''; role:'instructor'|'empresa'='instructor'; constructor(private auth:AuthService, private router:Router){} async login(){ try{ await this.auth.login(this.email,this.password); if(this.role==='instructor') this.router.navigateByUrl('/instructor'); else this.router.navigateByUrl('/company'); }catch(e:any){ alert(e.message||'Error'); } } }
